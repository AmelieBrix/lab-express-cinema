 {{#each details}}
         <div>
             <h2>{{title}}</h2>
             <img src="{{image}}" alt="">
            <p>{{stars}}</p>
            <p>{{description}}</p>
            <p>{{showtimes}}</p>
        </div>
    {{/each}}
</div>